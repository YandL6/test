# === Hermes Agent 依赖（由 setup_hermes.sh 自动安装）===
# Hermes 本身的依赖在 hermes-agent/pyproject.toml 中，无需在此重复

# === API Gateway 层依赖 ===
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-multipart==0.0.9
sse-starlette==2.0.0
aiofiles==23.2.1
aiosqlite==0.21.0

# === 工具库 ===
httpx==0.27.0
python-dotenv==1.0.1
loguru==0.7.2
